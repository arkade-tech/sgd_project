# SGD Project

## 🛠 Automated Setup
Instead of manually creating the environment, you can simply run the build script:

```bash
# 1. Run the build script (creates venv & installs deps)
./build.sh

# 2. Run the project
./venv/bin/python main.py
```
